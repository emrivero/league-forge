import { Box } from "@mui/material";
import { NoData } from "../../components/no-data";

export const Rules = () => {
  return (
    <Box>
      <NoData title={"En construcción..."} />
    </Box>
  );
};
